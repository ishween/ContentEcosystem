# Skill changelog

Running log of skill changes applied by `/reflect`. Every entry here corresponds to
its own git commit, so `git log -- .claude/skills/` gives the full history alongside
this summary.

Format: `<date> - <skill> - <what changed> - evidence: <slugs>`

(No entries yet. This fills in once `/reflect` finds a pattern confirmed across two
or more published scripts.)
