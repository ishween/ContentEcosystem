Generated output lives here as `artifacts/<slug>/`, created by `/run-pipeline`. Do
not hand-edit files in here; edit a copy under `feedback/published/<slug>/` instead
so the feedback loop can learn from the diff.
